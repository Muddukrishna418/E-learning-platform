import { describe, expect, it } from 'vitest';
import { MyCourses } from './my-courses';

describe('MyCourses', () => {
  it('is defined as an Angular component', () => {
    expect(MyCourses).toBeDefined();
  });
});
